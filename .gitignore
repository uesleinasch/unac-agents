
# Switchboard kanban database — machine-local, do not commit.
.switchboard/kanban.db
.switchboard/*.db-shm
.switchboard/*.db-wal

# Node modules — never commit
node_modules/

# CLAUDE.md é ignorado pelo gitignore global do usuário; rastrear neste repo.
!CLAUDE.md
